# Core
streamlit>=1.32.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Embeddings — BGE-small (same 384-dim as MiniLM, better retrieval quality)
sentence-transformers>=2.7.0
torch>=2.0.0               # CPU-only is fine; install torch+cpu for lighter install

# Vector DB
pinecone>=3.0.0

# LLM + orchestration
langchain>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0

# Local LLM (Ollama must be installed separately)
# ollama                   # installed via: https://ollama.com/download
# After install: ollama pull llama3

# Utilities
tqdm>=4.66.0
